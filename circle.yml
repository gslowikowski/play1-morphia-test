machine:
  environment:
    _JAVA_OPTIONS: "-Xms512m -Xmx1024m"
  java:
    version: oraclejdk6

dependencies:
#  cache_directories:
#    - mongodb-linux-x86_64-2.6.4
  pre:
#    - if [[ ! -d mongodb-linux-x86_64-2.6.4 ]]; then wget http://downloads.mongodb.org/linux/mongodb-linux-x86_64-2.6.4.tgz && tar xvzf mongodb-linux-x86_64-2.6.4.tgz; fi
#    - sudo /etc/init.d/mongodb stop
#    - stop mongodb
#    - sudo cp mongodb-linux-x86_64-2.6.4/bin/* /usr/bin
#    - sudo /etc/init.d/mongodb start
    - start mongodb
  override:
    - java -XX:+PrintFlagsFinal -version
    - mvn --version
    - mvn test-compile -Pautodeps,play13 --fail-never --update-snapshots --settings .travis.settings.xml -e
  post:
    - stop mongodb

test:
  override: 
    - mvn test -Pautodeps,play13 --settings .travis.settings.xml -e
