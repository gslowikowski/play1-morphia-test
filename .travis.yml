language: java

services:
  - mongodb

cache:
  directories:
    - $HOME/.m2

install:
  - java -XX:+PrintFlagsFinal -version
  - mvn --version

script:
  - mvn test -Pautodeps,play13 --update-snapshots --settings .travis.settings.xml -e

jdk: oraclejdk7
